/**
 * Shared product copy — gentle, consistent check-in language (dashboard + landing).
 */
export const CHECKIN_INVITE = "Add a check-in";

/** Button label for updating an existing snapshot. */
export const CHECKIN_AGAIN_BUTTON = "Check in again";
